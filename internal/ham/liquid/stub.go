//go:build !liquid

// Package liquid provides the liquid-dsp OFDM modem adapter.
// Build with -tags liquid to enable it.
// Without the tag this package is empty — no symbols, no CGO dependency.
package liquid
